# Exceptions for ``scripts/check_test_layer_imports.py`` (PR-T5, plan 008).
# PR-T6 cleared prior rows by moving cross-layer tests to ``tests/scenarios/`` and
# fixing narrow-layer tests. Add [[allow_prefix]] only with a short justification.
