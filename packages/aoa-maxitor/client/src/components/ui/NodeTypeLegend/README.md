# NodeTypeLegend

**Role:** Scrollable legend for G6 node types (icons from `@/lib/icons`) inside the graph viewport.

## Files

| File | Responsibility |
|------|----------------|
| `NodeTypeLegend.tsx` | Renders `legend_items`-style `{ type, color }` rows. |
