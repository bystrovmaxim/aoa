# MainLayout

**Role:** Application chrome — permanent left drawer for navigation plus flexible main column.

## Files


| File             | Responsibility                         |
| ---------------- | -------------------------------------- |
| `MainLayout.tsx` | MUI `Drawer` + main slot (`children`). |
