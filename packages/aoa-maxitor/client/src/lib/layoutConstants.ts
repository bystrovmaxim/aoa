// src/lib/layoutConstants.ts
/** Persistent navigation drawer width (px). */
export const DRAWER_WIDTH = 300;

/** Drawer / left rail background (shared by ``MainLayout`` and ``LeftSidebar`` chrome). */
export const SIDEBAR_SURFACE = "#EBF2F5";
