# tests/auth/__init__.py
"""
Тесты системы аутентификации и авторизации ActionMachine.

═══════════════════════════════════════════════════════════════════════════════
НАЗНАЧЕНИЕ
═══════════════════════════════════════════════════════════════════════════════

Покрывает все компоненты аутентификации:

- check_roles — декоратор уровня класса для объявления ролевых ограничений.
  Записывает спецификацию ролей в cls._role_info. Проверяет, что целевой
  класс наследует RoleGateHost. Поддерживает четыре формата spec:
  ROLE_NONE, ROLE_ANY, конкретная роль (строка), список ролей.

- AuthCoordinator — координатор процесса аутентификации. Объединяет
  три компонента: CredentialExtractor → Authenticator → ContextAssembler.
  Последовательно извлекает учётные данные, проверяет их и собирает Context.

- NoAuthCoordinator — провайдер для открытых API. Всегда возвращает
  анонимный Context с пустым UserInfo (user_id=None, roles=[]).

- Константы ROLE_NONE и ROLE_ANY — специальные строковые маркеры
  для декоратора @check_roles.

Проверка ролей в рантайме (ActionProductMachine._check_action_roles)
покрыта в tests/core/test_machine_roles.py. Здесь тестируется только
декоратор @check_roles (запись метаданных) и координаторы аутентификации.
"""
